{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build",
            "type": "shell",
            "command": "cmake -B build -DCMAKE_BUILD_TYPE=Debug ; cd build; ninja"
        }
    ]
}